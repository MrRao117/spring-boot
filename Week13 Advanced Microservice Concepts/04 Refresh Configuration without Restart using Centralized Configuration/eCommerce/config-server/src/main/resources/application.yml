spring.application.name: config-server
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MrRao117/ecommerce-config-server
          username: mrrao117
          password: ${GITHUB_TOKEN}
          default-label: master
server:
  port: 8888
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka